{
  "description": "Holds back git commands that destroy work git cannot recover: force-pushes, remote branch deletion, hard resets, forced cleans, and the commands that throw away uncommitted changes. Lexes the command and inspects only the arguments of an actual git subcommand, so ordinary shell containing -f or the word push is unaffected, and a ';' inside a commit message is not a command separator.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/git-guard.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
