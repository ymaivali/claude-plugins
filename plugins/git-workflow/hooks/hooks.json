{
  "description": "Blocks git commands that destroy work git cannot recover: force-pushes, remote branch deletion, hard resets, forced cleans and forced checkouts. Parses the command and inspects only the git subcommand's arguments, so ordinary shell containing -f or the word push is unaffected.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/hooks/git-guard.py",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
